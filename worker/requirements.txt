fastapi==0.115.6
uvicorn[standard]==0.34.0
faster-whisper==1.1.1
boto3==1.35.90
requests==2.32.3
redis==5.2.1
rq==2.1.0
# Deliberately unpinned: YouTube frequently changes its site in ways that
# break older yt-dlp releases, so this needs to track upstream's latest
# rather than a fixed version like the rest of this file.
yt-dlp
# JS challenge solver yt-dlp needs to pass YouTube's current anti-bot
# checks — without this, extraction fails even with valid cookies and a
# JS runtime (deno) present.
yt-dlp-ejs
# Proof-of-origin token provider — a second, independent line of defense
# against YouTube's bot-check (unlike cookies/player-client tricks, this
# doesn't rely on IP reputation). Talks to the bgutil-pot service over
# HTTP (see docker-compose.yml, worker/config.py, worker/youtube_ingest.py).
bgutil-ytdlp-pot-provider
